
class User{
    name;
    surname;
    age;

    constructor(name, surname, age){
        this.name = name;
        this.surname = surname;
        this.age = age;
    }

    createUser(){
        // query SQL DB consulta
        // query SQL DB crea
    }
}


// Controlador

// datos del formuylario
User.createUser(name, apellido, edad)